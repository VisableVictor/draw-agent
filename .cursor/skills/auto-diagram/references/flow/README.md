# Flow Phase

`flow/` 只负责前半段的输入收敛，不负责后端选择和交付。

## 子阶段

1. [brainstorm/README.md](brainstorm/README.md)
2. [intake/README.md](intake/README.md)

## 顺序纪律

- 先 `brainstorm`，后 `intake`
- `intake` 结束前必须形成 unified spec
- 不要在 `flow/` 阶段提前读 backend contract 或质量脚本
