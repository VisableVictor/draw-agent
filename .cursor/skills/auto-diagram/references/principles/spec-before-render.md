# Spec Before Render

- unified spec 必须先于任何具体后端语法
- 先定内容和图型，再定视觉；禁止先选模板再塞内容
- 主题包只服务视觉层，不得替代内容判断、图型判断和布局判断
- 会进入正式渲染的任务，默认先给一版草稿图确认最终画面，再开始重渲染
- 草稿图必须用自身外形表达当前比例；不能只在文字里写 `16:9`，却交付一个竖长线框
- 正式图法任务优先走正式图法，不要用自由造型假装标准图
- 默认 SVG-first，但不是所有图都必须走 SVG
