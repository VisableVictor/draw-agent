# Report Grade Output

- 面向汇报的图优先追求“清晰、秩序、可讲”，不是炫技
- 节点尺寸按最坏文本长度决定，不能先画小盒子再硬塞字
- 任何有边框的文本卡片都必须在卡片内折行，不得正文越界
- 所有主图元素都应当落在统一坐标系或统一语义结构里
- 箭头头部默认克制，不允许大三角盖住内容
- 主图完成后必须走 post-check；发现结构性错误先修图，再重跑校验，再允许交付
- 用户未确认前，不要默认额外导出 `pptx`；除非用户一开始就明确要求
